article
  header
    section#navigation
    h1 archives
  section.body
    #archive.inner
      ul
        - _(site.getPosts()).chain().clone().reverse().each(function(post){
          li
            a(href=`/${post.htmlPath()}`) #{post.title()}
        - })
