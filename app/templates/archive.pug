article#archive

  - var groups = _(site.getPosts()).chain().clone().groupBy(function(post){
  -   return post.attributes.date.split('-')[0]
  - }).value()

  h1
    a(href='/archive.html' title=`Permanent link to ‘Archive: blog.davemo.com’`) Previously, on blog.davemo.com
  h6
    time()
      time 2008 - #{new Date().getFullYear()}
  - _(Object.keys(groups).reverse()).each(function(year) {
    h2 #{year}
    ul
      - groups[year].reverse().forEach(function(post) {
        li
          a(href=`/${post.htmlPath()}`) #{post.title()}
      - })
  - })